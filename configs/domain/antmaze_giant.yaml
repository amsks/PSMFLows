# @package _global_
defaults:
  - /agent: fb_flowbc
  - _self_

# Matches td_jepa's sweep_antmaze (FBFlowBCAgent + bc_coeff=0.3).
domain: antmaze-giant-navigate-v0
agent: fb_flowbc
bc_coeff: 0.3
ortho_coef: 1.0
lr_b: 1.0e-4
